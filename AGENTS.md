# 项目协作规则

## 通用文件规则

1. 所有新建和修改文件必须保存为 UTF-8 编码。读取和写入时显式使用 UTF-8，不得改变现有中文文本编码。
2. 文件写入必须分块执行。每次写入不超过 300 行且不大于 12000 字。
3. 不在页面上堆放业务规则、实现细节或对当前任务无帮助的说明文字。
4. 业务规则应通过控件限制、字段校验、提交确认和失败反馈体现，不使用常驻废话文案。

## YYB 脚本适配原则

1. 适配前必须先找到并完整阅读对应原脚本，再阅读 YYB 项目中相关接口的实现和测试。
2. 默认只修改“旧微信服务到 YYB”的桥接层。除非用户明确要求，不重写、不简化、不整理原业务代码。
3. 原脚本必须保留以下内容：
   - 小程序 AppID、业务域名和全部业务接口；
   - HTTP 方法、请求头、请求体、查询参数和字段名；
   - 签名算法、固定常量、调用顺序、等待、重试和失败判断；
   - 签到、任务、积分、注册、查询、通知等业务流程；
   - 原环境变量、JSON 输入、手动 token、缓存 token 等兼容入口；
   - 原日志收集、运行耗时和青龙 `sendNotify` 能力。
4. 不得因为代码看起来多余就删除原逻辑，也不得根据接口名称猜测新业务流程。
5. 原脚本本身存在限制时应原样保留并明确记录，不得伪造成 YYB 已解决。

## YYB 输入约定

1. YYB 账号变量默认使用 `YYB_SERVER=服务器地址@账号ID或OpenID`。
2. 多账号兼容换行或 `&` 分隔。
3. YYB 账号末尾可使用 `#备注`，但不得因此破坏原脚本已有的 `#`、`|`、JSON 或 token 输入格式。
4. 服务器地址没有协议时补 `http://`，有 `http://` 或 `https://` 时原样使用，并移除末尾多余 `/`。
5. 所有本地账号缓存必须使用规范化的 `server@ref` 作为键，避免多台 YYB 服务器的相同账号编号串号。
6. 原版的手动 token 缓存键必须保持稳定，不能强制要求存在 YYB 地址。

## YYB 接口对应

YYB 标准请求字段使用 `ref` 和 `app_id`：

- 微信登录 code：`POST /wxapp/getCode`
- 微信手机号授权：`POST /wxapp/getPhoneNumber`
- YYB 账号普通用户信息：`GET/POST /wx/getuserinfo`
- 通用微信操作：`POST /wxapp/operateWxData`，并传完整 `payload`

适配时遵守：

1. 先确认原服务实际返回的微信数据类型，再选择对应 YYB 接口。
2. 手机号授权只能对应 `getPhoneNumber`，不得用普通 `getuserinfo` 数据代替。
3. 原业务接口需要 `code`、`iv`、`encryptedData` 时，必须从真实 YYB 手机号授权响应中逐字段提取。
4. 响应兼容包装层时，应检查 `result`、`data.result` 以及其中的 `raw`，但不得把错误消息 `success` 当成 code。
5. `operateWxData` 必须沿用原脚本或真实抓包中的完整 payload。源码没有 payload 时不得编造。
6. 不得擅自把 `app_id` 改成 `appid`；只有目标接口源码明确要求时才使用其他字段名。

## 修改流程

1. 建立原脚本与适配脚本的对应关系。
2. 列出原微信服务调用、输入格式、缓存方式和所有业务流程。
3. 仅替换微信服务初始化、取 code、取手机号或通用微信操作部分。
4. 将适配结果写入单独目录，不覆盖原脚本。
5. 对照原版检查业务接口、字段、签名、流程、回退方式和提示是否仍然存在。
6. 对全部修改文件运行 JavaScript 语法检查和严格 UTF-8 检查。
7. 搜索旧服务残留、简化版 Env、裸 openid 缓存键和把 `success` 当 code 的宽松解析。
8. 没有连接真实 YYB 和目标业务服务测试时，必须标记为“静态检查通过，真实联调未验证”。

## 禁止事项

- 禁止修改 Downloads 中的原脚本。
- 禁止为了适配 YYB 改动无关业务逻辑或统一原作者代码风格。
- 禁止删除原环境变量和手动 token 兜底。
- 禁止凭空补注册、手机号、签名、云函数或加密参数。
- 禁止把目标业务服务的失败误报成 YYB 取码成功。
- 禁止在未经真实联调时宣称脚本可正常注册、签到或完成任务。
